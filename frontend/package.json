{
  "name": "moodbite-frontend",
  "private": true,
  "version": "0.2.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @moodbite/client",
    "build": "npm run build --workspace @moodbite/client",
    "typecheck": "tsc -p tsconfig.json && npm run typecheck --workspace @moodbite/client",
    "lint:arch": "steiger ./apps/client/src",
    "gen:api": "openapi-typescript ./openapi.json -o ./packages/api-client/src/schema.d.ts"
  },
  "devDependencies": {
    "openapi-typescript": "^7.5.0",
    "steiger": "^0.6.0",
    "typescript": "^5.7.2"
  }
}
